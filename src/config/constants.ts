const Moleculer = 'moleculer' as const;
const HttpClient = 'httpClient' as const;
const Kafka = 'kafka' as const;
const Mongo = 'mongo' as const;

export const PATHS = {
  Moleculer,
  HttpClient,
  Kafka,
  Mongo,
};
